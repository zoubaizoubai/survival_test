# 仓库贡献指南

## 项目结构与模块组织

这是一个 Godot 4.7 游戏项目，入口场景为 `scenes/home.tscn`，主要玩法位于 `scenes/game.tscn`。逻辑脚本按节点或功能放在 `scripts/` 中；每个 `.gd.uid` 文件应与对应脚本一同保留。代理与任务配置位于 `.agents/`、`.codex/` 和 `.beads/`。

## 构建、测试与开发命令

- `godot --editor --path .`：在编辑器中打开项目。
- `godot --path .`：运行主场景。
- `godot --headless --path . --quit`：无界面验证项目能否加载。

请使用 Godot 4.7；仓库目前没有导出预设或自动化测试运行器。

## 编码风格与命名约定

遵循现有 GDScript 风格：使用 Tab 缩进、类型化签名、函数间空两行，并在多行集合末尾保留逗号。文件和成员使用 `snake_case`，预加载脚本常量使用 `PascalCase`，普通常量使用 `UPPER_SNAKE_CASE`。资源路径使用 `res://`；条件允许时，通过 Godot 编辑序列化场景。

## 测试规范

先运行无界面加载检查，再手动验证移动、暂停、升级、战斗及胜负流程。界面改动需在 1280×720 基准视口下测试触摸和鼠标输入。未来的测试放在 `tests/` 中，命名为 `test_<feature>.gd`，并记录运行命令。

## 提交与拉取请求规范

提交标题应简短并使用祈使语气。采用 `<type>: <summary>` 格式，例如 `fix: clamp enemy spawn position`，且每个提交只包含一项完整改动。拉取请求应说明行为变化、关联 Beads 任务、列出验证结果；视觉改动还需提供截图或视频。

## 任务收尾流程

完成实现并通过测试后，再运行 `bd close <id> --reason="..."`。关闭任务必须发生在最终暂存之前；随后检查 `git status`，将实现文件与生成的 `.beads/*.jsonl` 审计记录一同暂存，执行最终检查并创建一次 Git 提交。不要把“已有 Git 提交”写入 Beads 验收条件或关闭原因；在提交信息中引用任务 ID。
可以自动提交, 但不允许自动推送远程.

<!-- BEGIN BEADS CODEX SETUP: generated by bd setup codex -->
## Beads 任务跟踪

本仓库使用 Beads（`bd`）持久跟踪任务。工作流说明位于项目技能 `.agents/skills/beads/SKILL.md` 或全局技能 `~/.agents/skills/beads/SKILL.md`；任务操作统一使用 `bd` CLI。

只有明确需要改动代码时才需要记录任务；一般对话或不涉及代码改动的工作无需建立任务。

### 常用命令

```bash
bd ready                # 查找可执行任务
bd show <id>            # 查看任务详情
bd update <id> --claim  # 认领任务
bd close <id>           # 完成任务
bd prime                # 刷新 Beads 上下文
```

### 规则

- 所有任务都通过 `bd` 跟踪，不要创建 Markdown TODO 列表。
- Beads 上下文缺失或过期时运行 `bd prime`。Codex 0.129.0 及以上版本可通过原生钩子自动加载上下文；使用 `/hooks` 检查或切换钩子。
- 使用 `bd remember` 将长期项目记忆保存在 Beads 中，不要创建临时记忆文件。

**架构概述：**任务保存在本地 Dolt 数据库中，通过 Git 远端的 `refs/dolt/data` 同步；`.beads/issues.jsonl` 只是被动导出。详细说明和反模式参见 https://github.com/gastownhall/beads/blob/main/docs/SYNC_CONCEPTS.md。
<!-- END BEADS CODEX SETUP -->
